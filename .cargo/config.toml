# Cargo configuration for autodiff/Enzyme support

[build]
# Autodiff (Enzyme) support for pricer_kernel crate
# Uncomment for nightly builds with autodiff:
# rustflags = ["-Z", "autodiff=Enable"]

[target.x86_64-unknown-linux-gnu]
# Additional optimization flags for release builds
# rustflags = ["-C", "target-cpu=native"]
